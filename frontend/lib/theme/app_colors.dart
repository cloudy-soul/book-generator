import 'package:flutter/material.dart';

class AppColors {
  // Warm Wood Tones (Tradition & Grounding)
  static const Color burntUmber = Color(0xFF8B7355);     // Primary wood
  static const Color tawnyBirch = Color(0xFFA68A64);     // Secondary wood
  
  // Muted Greens (Growth & Calm)
  static const Color sageGreen = Color(0xFF87A96B);      // Success states
  static const Color deepMoss = Color(0xFF2E4600);       // Important text
  
  // Deep Reds (Passion & Focus)
  static const Color bibliophileRed = Color(0xFF9D2933); // Primary CTAs
  static const Color berryAccent = Color(0xFFC71585);    // Hover states
  
  // Gentle Blues (Clarity & Trust)
  static const Color serenity = Color(0xFF91A8D0);       // Info sections
  static const Color deepWisdom = Color(0xFF034F84);     // Links & shadows
  
  // Soft Pastels (Playfulness & Light)
  static const Color roseQuartz = Color(0xFFF4C2C2);     // Celebrations
  static const Color lavenderWeb = Color(0xFFE6E6FA);    // Background highlights
  
  // Neutral Foundation
  static const Color oldLace = Color(0xFFFDF5E6);        // Canvas (never pure white)
  static const Color timberwolf = Color(0xFFD3D3CB);     // Disabled states
  
  // Semantic Colors
  static const Color success = sageGreen;
  static const Color warning = Color(0xFFFFB74D);
  static const Color error = Color(0xFFE57373);
  static const Color info = serenity;
}